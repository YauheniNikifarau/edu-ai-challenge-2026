"""MCP server entrypoint — full bootstrap lands in Story 1.4."""
import sys

if __name__ == "__main__":
    print(
        "configuration not loaded: full server bootstrap lands in Story 1.4",
        file=sys.stderr,
    )
    sys.exit(1)
